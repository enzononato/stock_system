# Dependências do backend (FastAPI + PyMySQL). Arquivo em UTF-8 sem BOM.
# Reescrito para remover resíduos do antigo app desktop (Tkinter/matplotlib/numpy/
# pillow/mysql-connector-python) que não são usados por esta API.

# Framework web e servidor ASGI
fastapi==0.115.0
starlette==0.38.6
uvicorn[standard]==0.30.6
anyio==4.13.0
h11==0.16.0
httptools==0.7.1
watchfiles==1.1.1
websockets==16.0
click==8.3.2
colorama==0.4.6
python-multipart==0.0.12

# Configurações e validação
pydantic==2.9.2
pydantic-settings==2.5.2
pydantic_core==2.23.4
annotated-types==0.7.0
typing_extensions==4.15.0
python-dotenv==1.2.2

# Banco de dados MySQL (SQL cru via PyMySQL) + pool de conexões
PyMySQL==1.1.1
DBUtils>=3.1.0

# Autenticação (JWT), hashing de senha e rate limiting do login
bcrypt==4.2.0
python-jose[cryptography]==3.3.0
cryptography==46.0.2
cffi==2.0.0
pycparser==2.23
ecdsa==0.19.2
rsa==4.9.1
pyasn1==0.6.3
slowapi>=0.1.9
limits==5.8.0
Deprecated==1.3.1
wrapt==2.3.0

# Geração de documentos Word (termos de responsabilidade e devolução)
python-docx==1.1.2
lxml==6.0.1

# Armazenamento em nuvem (S3), usado quando STORAGE_BACKEND=s3
boto3==1.35.22
botocore==1.35.99
s3transfer==0.10.4
jmespath==1.1.0

# Utilitários HTTP/rede e diversos, usados pelas libs acima
idna==3.11
urllib3==2.6.3
packaging==25.0
PyYAML==6.0.3
